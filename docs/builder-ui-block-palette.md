# Builder UI Block Palette

The prototype block palette lists the first builder-ready blocks:

- `core/heading`
- `core/content-text`
- `commerce/product-price`
- `commerce/archive-links`

Clicking a palette item inserts a block node into the active layout section with default props.
