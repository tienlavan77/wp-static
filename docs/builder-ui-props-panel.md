# Builder UI Props Panel

The props panel edits the selected block node.

Current scope:

- Text props.
- Number props.
- Immediate layout state updates.
- Live preview refresh after each edit.

Schema-driven controls arrive after the builder is connected to theme block libraries.
