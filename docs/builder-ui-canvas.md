# Builder UI Canvas

The prototype canvas edits the current layout section.

Supported actions:

- Select a block node.
- Move a block up.
- Move a block down.
- Remove a block.

The canvas keeps output compatible with the Phase 24 layout JSON model.
