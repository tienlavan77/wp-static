# Builder UI Live Preview

The builder prototype renders a live iframe preview from the in-memory layout.

Current preview supports:

- Heading blocks.
- Content text blocks.
- Product price blocks with sample pricing.
- Archive link blocks with slug-only URLs.

The production preview will use the Phase 25 `renderLayout()` pipeline.
