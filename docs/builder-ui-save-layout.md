# Builder UI Save Layout

The builder prototype can export the current layout as JSON.

The `Save JSON` action downloads the in-memory layout document with:

- `id`
- `name`
- `version`
- `contentTypes`
- `sections`

Server-side save, auth, revisions, and draft/publish state are reserved for the production builder phase.
