# Package Compatibility Layer

`packages/` contains workspace packages and stable re-export shims. Their
paths and package contracts remain stable for consumers; implementation stays
owned by `framework/src/`.
