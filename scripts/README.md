# Development Scripts

`scripts/` contains developer and release utilities that are not public WPSC
CLI commands. They may compose Framework modules, but must not become a second
business-logic entry point.
