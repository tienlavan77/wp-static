# Historical Outputs

`outputs/` preserves sprint reviews, implementation diffs, audits, roadmap
snapshots and generated project reports. These records are historical evidence
and are intentionally retained at stable paths.

Current documentation belongs in `docs/`. Executable assets do not belong in
this folder.
