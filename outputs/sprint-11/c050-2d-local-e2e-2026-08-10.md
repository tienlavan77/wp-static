# C050-2D Local End-to-End Validation

```text
C050-2D = PASS
C040 build: PASS
C041 verification: PASS
C050 product verify: PASS
C050 publish dry-run: PASS
C050 local publication: PASS
C050 idempotency: PASS
C050 conflict: PASS (focused publisher coverage)
C050 verify-installation: PASS
CWD independence: PASS
CLI confirmation boundary: PASS
Security: PASS
VPS mutation: NONE
Production publication: NONE
C049 semantics: UNCHANGED
C048 semantics: UNCHANGED
```

The E2E fixture generates an ephemeral Ed25519 key pair, builds a signed C040
package, creates a transport bundle with the existing bundler and invokes the
public root CLI from an unrelated temporary CWD. No production key, artifact,
Installation or distribution path is used.

Validated public flow:

```text
C040 local build
-> C041-backed CLI verify
-> publish without confirmation rejected
-> dry-run with zero destination mutation
-> local atomic publication
-> identical repeat returns IDEMPOTENT_SUCCESS
-> explicit local Installation verification through Registry identity
```

During E2E validation a real boundary bug was found: the publisher returned a
`DRY_RUN` result with `ok: false`, causing a non-zero CLI exit. The publisher now
returns `ok: true` for `DRY_RUN` while preserving `mutation: NONE` and zero
filesystem mutation.

## Tests

C050-2D focused:

```text
tests 2
pass 2
fail 0
cancelled 0
skipped 0
```

C050-2A/2B/2C/2D combined:

```text
tests 16
pass 16
fail 0
cancelled 0
skipped 0
```

`git diff --check`: PASS.

No SSH, sudo, C048, C049 update, Runtime restart, systemd operation or
production publication was executed.
