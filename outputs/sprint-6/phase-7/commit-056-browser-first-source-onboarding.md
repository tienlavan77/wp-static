# Commit 056 - Browser-First Source Onboarding

Customer setup is Browser-only. Runtime configuration is deployment plumbing;
it must never require customers to export credentials, edit env files, or run
commands. WordPress and WooCommerce credentials are submitted once through the
Dashboard and stored privately per Site after validation.
