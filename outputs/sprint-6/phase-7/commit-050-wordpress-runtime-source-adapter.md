# Commit 050 - WordPress Runtime Source Adapter

## Delivered

- Added `wordpress` Source Adapter v2 with lifecycle validation, WordPress REST
  authentication, content reads, and configurable webhook registration bridge.
- Added default `wordpress` provider to generated Runtime config.
- Added Runtime Content Reader, which reads durable Source metadata rather than
  Browser input when Build runs.
